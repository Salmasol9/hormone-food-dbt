name: 'hormone_food_dbt'
version: '1.0.0'

profile: 'hormone_food_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  hormone_food_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table